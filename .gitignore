# =============================================================================
# Airymax TUI — .gitignore（agentrt-tui，Rust 二进制 crate）
# 策略：非必要不追踪。仅忽略本地构建产物与运行时数据，
#       源码与必要配置保持可追踪，避免误推远程仓库
# =============================================================================

# ---------- Rust 构建产物 ----------
target/
**/target/

# ---------- 离线 vendor 配置（由 build.sh 从预置包生成，不入库） ----------
.cargo/config.toml

# ---------- Rust 分析 ----------
*.rs.bk

# ---------- IDE 与编辑器配置 ----------
.vscode/
.idea/
.vs/
.trae/
*.code-workspace

# ---------- 构建目录 ----------
build/
build-*/

# ---------- 环境与密钥（禁止入库） ----------
.env
.env.*
!.env.example
*.pem
*.key
*.p12

# ---------- 运行时数据 ----------
*.log
*.sock
*.db
*.tmp
*.bak

# ---------- 操作系统文件 ----------
.DS_Store
Thumbs.db
Desktop.ini

# ---------- 编辑器临时文件 ----------
*.swp
*.swo
*~

# 注：TUI 为二进制 crate，Cargo.lock 需入库以保证可复现构建，故不忽略
